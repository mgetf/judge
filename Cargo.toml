[package]
name = "judge"
version = "0.1.0"
edition = "2021"
description = "Event-sourced court: the log is history, the reducer is the constitution."
license = "MIT"
publish = false

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
thiserror = "2"
